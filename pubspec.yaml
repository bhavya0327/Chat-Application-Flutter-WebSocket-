name: chat_app_flutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # Add web_socket_channel for WebSocket communication
  web_socket_channel: ^3.0.1

  cupertino_icons: ^1.0.8

  shared_preferences: ^2.0.15 # Add this line for storing username locally

  # Add the intl package for date formatting
  intl: ^0.18.0  # <-- Add this line for DateFormat

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/ # This line includes all images inside the assets/images folder
